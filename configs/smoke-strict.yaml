model:
  name: /home/tutu/tinyvr/models/Qwen/Qwen2___5-1___5B-Instruct
  prompt_format: chat

dataset:
  name: gsm8k
  config: main
  split: test
  revision: null
  max_examples: 16
  selection: first
  dataset_seed: 42
  prompt_template: strict

sampling:
  num_samples: 8
  generation_seed: 42
  temperature: 0.7
  top_p: 0.95
  max_new_tokens: 512
  batch_size: 4

reward:
  correct_answer_reward: 1.0
  incorrect_answer_reward: 0.0
  format_compliance_reward: 0.0
  length_penalty_per_token: 0.0

output:
  run_id: gsm8k-qwen-smoke-C
  output_dir: outputs/gsm8k-qwen-smoke-C
  keep_failed_run: false

analysis:
  strict: false
  k_values: [1, 4, 8]
  write_plots: true
